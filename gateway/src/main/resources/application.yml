server:
  port: 8100

spring:
  cloud:
    gateway:
      routes:
        - id: images
          uri: http://localhost:8081
          predicates:
            - Path=/images/**
          filters:
            - PrefixPath=/api/v1